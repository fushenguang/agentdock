# This file is intentionally minimal.
# Auth token for npm registry publishing is injected dynamically in CI
# (see .github/workflows/release.yml) rather than stored here.
#
# Local development does NOT need an auth token.
